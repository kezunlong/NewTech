@model NewTech.Model.Project

<div class="thumbnail">
    <img src="@Url.Content(Model.ThumbImage)" alt="...">
    <div class="caption">
        <h3>@Html.ActionLink(Model.Name, "Item", "Portfolio", new { Id = Model.Id }, null)</h3>
        <hr />
        @Model.Technologies
        <hr />
        <p>@Model.Description</p>
        <p>@Html.ActionLink("了解详情 >>", "Item", "Portfolio", new { Id = Model.Id }, null)</p>
    </div>
</div>